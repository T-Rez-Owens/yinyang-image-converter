# Yin-Yang Image Converter Configuration
# Edit these settings and run the script - no terminal input needed!

[images]
# Image paths (leave empty for auto-detection from bottomimage/topimage folders)
image1_path = 
image2_path = 

# Image source directories
bottomimage_directory = bottomimage
topimage_directory = topimage

[transformations]
# Rotation in degrees (positive = clockwise)
image1_rotation = 65
image2_rotation = 245

# Flip settings (true/false)
image1_flip_horizontal = false
image1_flip_vertical = false
image2_flip_horizontal = true
image2_flip_vertical = false

[processing]
# Unification methods to run (comma-separated)
# Options: brightness_match, histogram_match, color_balance, edge_enhance
methods = brightness_match, histogram_match, color_balance, edge_enhance

# Generate all methods (true) or just first method (false)
generate_all_methods = true

# Image resolution (pixels)
resolution = 400

# Yin-yang radius
radius = 1.0

[output]
# Output directory
output_directory = ../output

# Save with high quality
dpi = 300

# Show images after generation
show_images = true

# File naming prefix
filename_prefix = yinyang